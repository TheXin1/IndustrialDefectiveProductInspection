<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.industrialdefectiveproductinspection.mapper.ChatMessageMapper">
    <insert id="insertBatch">
        INSERT INTO chat_message (session_id, role, content, created_at)
        VALUES
        <foreach collection="messages" item="msg" separator=",">
            (#{sessionId}, #{msg.role}, #{msg.content}, #{msg.createdAt})
        </foreach>
    </insert>

    <delete id="deleteBySessionId">
        DELETE FROM chat_message WHERE session_id = #{sessionId}
    </delete>

    <select id="selectBySessionId" resultType="com.example.industrialdefectiveproductinspection.domain.ChatMessage">
        SELECT id, session_id AS sessionId, role, content, created_at AS createdAt
        FROM chat_message
        WHERE session_id = #{sessionId}
        ORDER BY id ASC
    </select>
</mapper>
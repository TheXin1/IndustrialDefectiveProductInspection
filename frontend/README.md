# Frontend (Vue 3 + Vite + Pinia + Element Plus)

## 开发环境
- Node.js 18+

## 运行
```bash
cd frontend
npm install
npm run dev
```

## 配置
- 默认后端代理为 `http://127.0.0.1:8080`
- 如需自定义 API 地址，可设置环境变量 `VITE_API_BASE_URL`

## 接口联调
- 登录：`/api/v1/auth/login`
- 推理检测：`/api/v1/inference/detect`
